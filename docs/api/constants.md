# `duq.constants`

CODATA-versioned fundamental physical constants as [`Quantity`][duq.Quantity]
objects. The default set is CODATA 2022, reached by attribute access on the
module (`duq.constants.k_B`) or explicitly via `codata`.

::: duq.constants
    options:
      members:
        - codata
        - ConstantSet
