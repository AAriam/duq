# `duq.jax`

The JAX back-end: `jit`/`grad`/`vmap`-safe unit-carrying arrays via quax
primitive interception. Requires the `duq[jax]` extra.

::: duq.jax
    options:
      members:
        - Quantity
        - grad
        - jacfwd
        - jacrev
        - hessian
        - jit
        - vmap
